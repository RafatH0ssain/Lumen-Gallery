# Secrets — never committed. This is the fix for the original leak.
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
!.dev.vars.example

# Dependencies & build output
node_modules/
dist/
.wrangler/

# Editor / OS noise
.DS_Store
*.local
.vscode/
.idea/
